using System.Collections;
using TMPro;
using UnityEngine;

public class InGameNotificationsHandler : MonoBehaviour
{
    //[SerializeField]
    //private TextMeshProUGUI[] _messages;

    //private readonly Queue _messageQueue = new();

    //public void OnMessageReceived(string message)
    //{
    //    Debug.Log($"OnMessageReceived {message}");

    //    _messageQueue.Enqueue(message);

    //    if (_messageQueue.Count > 3)
    //    {
    //        _messageQueue.Dequeue();
    //    }

    //    int queueIndex = 0;
    //    foreach (string qMsg in _messageQueue)
    //    {
    //        _messages[queueIndex++].text = qMsg;
    //    }
    //}
}
